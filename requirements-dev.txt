# Additional deps for training/verification/data-fetching scripts
# (train.py, verify.py, scripts/*) - not needed by the deployed API,
# not installed on Render. Install alongside requirements.txt for local dev:
#   pip install -r requirements.txt -r requirements-dev.txt
pandas==3.0.5
pyarrow==25.0.1
requests==2.34.2
matplotlib==3.11.1
tabulate==0.10.0
