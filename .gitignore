__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# regenerable via scripts/fetch_data.py - not vendored into git
# (anchored to repo root so it doesn't also swallow frontend/src/data/)
/data/

# local Ralph-loop run logs
sweep_run.log
