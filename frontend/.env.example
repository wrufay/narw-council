
# Base URL of the deployed (or local) FastAPI backend, e.g. https://narw-council.onrender.com
VITE_API_BASE_URL=http://localhost:8000

# Mapbox GL access token - required for the detection map to render.
# Get one free at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
